import React from 'react';
import ReactDOM from 'react-dom/client';   // yaha hum do library liye h jiski need h
// react dom for web app  
// react apne aap me khud ka dom bananta h
import App from './App';  


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  
    <App />
 
);   // render karo 
